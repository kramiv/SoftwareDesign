//and another character array query.??? what's that?
using namespace std;
#include<fstream>// pulling files from soemwhere else?
#include<string.h>

char1[];
char2[];
//char1[] = character
//char2[] = string to go through
int i;
while (i =! 0; i++) {
    a = _get_uchar(char1[i]);
    b = _get_uchar(char2[i]);
    if a == b {
        return i;
    } else {
        i = 0;
        return i;
    }
}