using namespace std;
#include<fstream>// pulling files from soemwhere else?
#include<string.h>
char1[];
char2[];
result[];

def concat(char1[], char2[]) {
    while (i =! 0; i++) {
    int a;
    a = _get_uchar(char1[i]);
    _put_uchar(result(a, i);
    }
    return i;
    b = 0;
    while (b =! 0; b++) {
    int z;
    z = _get_uchar(char2[b]);
    _put_uchar(result(z, b + i);
    }
    while (w =! 0; d++) {
        w = _get_uchar(result[d]);
        return d;
    }
}

//idk if this works. I need help. I decided to just write soemthing
// this time intsead of leaving it blank...